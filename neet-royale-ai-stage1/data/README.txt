# This folder is auto-generated by running the pipeline.
#
# Files generated here (all excluded from git):
#   registry.db     — Question bank (source_documents, extracted_pages, questions)
#   performance.db  — Match performance (match_sessions, match_answers)
#
# They are created automatically when you run:
#   python -m app.ingestion.downloader     (Stage 1)
#   uvicorn app.main:app                   (API startup also inits tables)
#
# NEVER commit *.db files — they contain local paths and runtime data.
